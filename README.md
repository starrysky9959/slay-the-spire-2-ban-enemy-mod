# Ban Enemy Mod

## English

`Ban Enemy Mod` is a `Slay the Spire 2` mod that lets players disable selected encounter groups before starting a run.

### Install

1. Download the latest release zip from GitHub Releases or the Cloudflare direct download link below.
2. Extract the included `BanEnemyMod` folder into your `Slay the Spire 2/mods/` directory.
3. Launch the game.
4. On character select, click `Ban Enemy` or press `B`.

### Downloads

- GitHub Releases: `https://github.com/starrysky9959/slay-the-spire-2-ban-enemy-mod/releases/latest`
- Cloudflare direct download: `https://slay-the-spire-2-ban-enemy-mod.r2.dev/BanEnemyMod-latest.zip`

### Build

```bash
export PATH="$HOME/.dotnet:$PATH"
dotnet restore
dotnet build
```

### Release Contents

A player release zip contains:

- `BanEnemyMod.dll`
- `BanEnemyMod.json`
- `BanEnemyMod.pck`

## 中文

`Ban Enemy Mod` 是一个《Slay the Spire 2》Mod，用于在开局前禁用指定战斗关卡。

### 安装

1. 从 GitHub Releases 或下方 Cloudflare 直链下载最新版本压缩包。
2. 将压缩包中的 `BanEnemyMod` 文件夹解压到 `Slay the Spire 2/mods/` 目录。
3. 启动游戏。
4. 在角色选择界面点击 `Ban Enemy / 禁用敌人`，或直接按 `B`。

### 下载地址

- GitHub Releases：`https://github.com/starrysky9959/slay-the-spire-2-ban-enemy-mod/releases/latest`
- Cloudflare 直链：`https://slay-the-spire-2-ban-enemy-mod.r2.dev/BanEnemyMod-latest.zip`

### 构建

```bash
export PATH="$HOME/.dotnet:$PATH"
dotnet restore
dotnet build
```

### 发布内容

面向玩家的发布包只包含：

- `BanEnemyMod.dll`
- `BanEnemyMod.json`
- `BanEnemyMod.pck`
